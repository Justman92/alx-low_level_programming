#include "main.h"

/**
 * _islower -> checks if the character islowercase
 * @c: a character argument
 * Return: return 1 and 0 depending on condition
 */

int _islower(int c)
{
	return (c >= 'a' && c <= 'z');
}
